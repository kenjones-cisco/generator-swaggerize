{
    "name" : "<%= slugName %>",
    "description" : "",
    "version" : "0.1.0",<% if (creatorName) {%>
    "author" : "<%= creatorName %> <<%= email %>>",<%}%>
    "contributors" : [],<% if (githubUser) {%>
    "repository" : {
        "type" : "git",
        "url" : "git://github.com/<%= githubUser %>/<%= slugName %>.git"
    },
    "bugs" : "http://github.com/<%= githubUser %>/<%= slugName %>/issues",
    "publishConfig" : {
        "registry" : "https://registry.npmjs.org"
    },<%}%>
    "engines" : {
        "node" : "~0.10.22"
    },
    "dependencies": {<% if (framework === 'express') {%>
        "express": "^4.0.0",
        "body-parser": "^1.9.0",
        "winston": "^1.1.0",
        "morgan": "^1.6.1",
        "swaggerize-express": "^3.0.0",<%}%><% if (framework === 'hapi') {%>
        "hapi": "^8.0.0",
        "swaggerize-hapi": "^1.0.0-",<%}%><% if (framework === 'restify') {%>
        "swaggerize-restify": "^2.0.0",
        "restify": "^3.0.3",<%}%>
        "mongoose": "^4.1.0",
        "lodash": "^3.10.0"
    },
    "devDependencies": {
        "tape": "^3.0.0",
        "jshint": "^2.4.1",
        "istanbul": "~0.2.3",
        "js-yaml": "^3.2.6",
        "enjoi": "~0.1.16"<% if (framework === 'express') {%> ,
        "supertest": "~0.13.0"<%}%><% if (framework === 'restify') {%> ,
        "supertest": "~0.13.0"<%}%>
    },
    "scripts": {
        "test": "tape tests/*.js",
        "cover": "istanbul cover tape -- tests/*.js",
        "lint": "jshint -c .jshintrc --exclude node_modules/"
    },
    "main" : "./server"
}
